# Template di riferimento — deploy_log_dashboard.sh genera /etc/nginx/conf.d/log-dashboard.conf
server {
    listen 80;
    server_name HOSTNAME _;

    root /opt/log-dashboard/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
