# PostgreSQL (stesso server dei Raspberry)
DB_HOST=172.20.1.84
DB_PORT=5432
DB_NAME=raspberry_counter
DB_USER=counter
DB_PASSWORD=CatisPg2026

# Percorsi (in installazione systemd usa path assoluti in /etc/win-log-aggregator.env)
AGGREGATOR_BASE_DIR=.
MACHINES_CONFIG=config/machines.yaml
STATE_FILE=state/offsets.json
POLL_INTERVAL_SEC=30
